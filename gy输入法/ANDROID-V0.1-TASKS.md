# GY 输入法 Android v0.1 施工任务拆解书

> 依据：ANDROID-DESIGN.md v2 · 工程原则：**质量优先于工期与体积，测试先行**（2026-08-05 产品决策）
> v0.1 目标：与微信输入法同机盲测手感不落下风的 26 键拼音输入法；零联网权限；
> 候选治理管线与桌面行为一致；映射层选词零错位。

---

## 一、v0.1 完成定义（DoD 总表）

**功能面**
- [ ] 26 键微信式键盘：卡片式按键、按压加深回弹、字符气泡、上滑角标、长按变体
- [ ] 删除键左滑快删 / 右滑恢复
- [ ] 拼音输入：组词、整句、组合中标点（首选+中文标点）、整句选词保留剩余拼音（只 a-z）
- [ ] 候选收起条（5 候选 + 「˅」+ 分隔线 + 模式标，GY VI 骨架）与 5 列展开面板（75 池 3 页）
- [ ] 简/EN 切换（取消预编辑不上屏、记忆中文模式）
- [ ] 密码框/验证码框自动 EN 直出、禁学习、无候选
- [ ] 横屏紧凑布局 + `onEvaluateFullscreenMode` 处理；全面屏手势 inset 适配

**质量面**
- [ ] 候选治理：无表情、无私用区、无符号、无重复、≤12 字、池上限 75
- [ ] 选词零错位（映射层单测 100% 分支覆盖）
- [ ] 键盘冷启动弹出 <300ms；按键→候选刷新 P95 <100ms；常驻内存 <80MB
- [ ] 飞行模式全流程可用；`adb shell dumpsys package` 证明无联网权限

**工程面**
- [ ] CI 绿：ktlint + detekt 零阻断 + 单测门禁
- [ ] 身份字段校验脚本通过（applicationId / IME service 名 / authorities 前缀）
- [ ] 真机矩阵（Pixel / 小米 / 华为 / OPPO 或 vivo，Android 10 / 12 / 14）验收签字

---

## 二、任务序列（严格依赖序）

### T0 · 工程脚手架与身份契约锁
- Gradle 标准工程（Kotlin DSL），代码落位仓库 `android/`
- **先锁身份字段再写业务**：applicationId、IME service 名、`authorities` 前缀 →
  `scripts/verify-identity.*` 校验脚本进 CI（§11.4 契约锁）
- CI 管线：ktlint（自动格式化）+ detekt（零阻断）+ JUnit 门禁；debug 签名与真机部署脚本
- **产出**：可安装的空 IME（能弹出自绘占位键盘，能走通「启用 → 设为默认」）
- **DoD**：CI 绿；校验脚本通过；安装/启用/设默认/切换回系统输入法全流程录屏存档

### T1 · librime JNI 桥（GYRimeBridge）
- 选型验证：Trime 预编译 librime（核查 boost 依赖、ABI 覆盖、minSdk 26），
  任一不满足即转自编译（NDK 构建入 CI 缓存）——**选型结论写成 ADR 存档**
- 桥 API（对齐 Mac GYRimeBridge 语义）：
  `init/deploy`、`processKey`、候选跨页收集（上限 75，对齐 `candidatesUpToCount:`）、
  `commitCandidateAtAbsoluteIndex`、简/繁模式切换、userdb 清空（回收站语义）
- rime-data 全量内置，与 `native/runtime/rime/shared/` 同源同版本（构建期校验哈希）
- **DoD**：instrumented 测试 `nihao` → 候选含「你好」；引擎冷启动就绪 <300ms（Pixel 级实测入库）

### T2 · 候选治理管线 + 索引映射（TDD，先于一切 UI）
> 实现状态（2026-08-05）：`:core` 已接入 app 依赖；生产代码以本地 Kotlin 编译器按 JVM 17
> 编译通过，8 个 JUnit 回归测试全绿。Gradle/Jacoco 分支覆盖报告待 JDK 17、Gradle 8.9 与
> 项目插件依赖可用后补齐；在报告达到 100% 前不得开始 T5。
- **纯 JVM Kotlin 模块**（不 import 任何 Android 类，可纯单测，Mac/Windows 未来可移植）
- 治理规则：纯 CJK 表意文字、≤12 字、滤表情/私用区/符号、去重、池上限 75
- 映射层：过滤后显示序号 → Rime 原始序号；提交只走绝对序号
- 测试先行：构造污染候选流（混入表情/重复/超长/私用区/繁体）断言过滤结果与映射一致性；
  **回归用例收录 Mac 事故词**（祢蚝/妮恏 类——过滤后选第 1 个必须提交正确词且学习加权正确）
- **DoD**：该模块单测分支覆盖 100%；不得在其测试全绿前开始 T5

### T3 · 键盘 View 体系
- 键位表驱动的自绘布局（数据描述键位，禁止写死坐标），26 键全键盘
- 按压瞬时加深/抬起回弹、字符放大气泡（微信同款位置与弹出时序，逐帧对比调）
- 主题表：浅/深/跟随系统；按键色、按压色、气泡色全部进表（§3.8 不写死）
- 键盘高度三档数据结构预留（v0.1 无设置 UI，默认标准）；横屏紧凑布局
- **DoD**：与微信输入法同机并排截图（浅/深 × 竖/横）键位、气泡、按压反馈一致

### T4 · 手势族与冲突裁决
- 手势：上滑角标、长按变体（大写+常用符号）、**删除键左滑快删/右滑恢复**（v0.1 核心）
- **手势冲突裁决状态机**：位移阈值 / 时间窗 / 方向锁，纯 JVM 实现 + 单测
  （例：删除键上短滑=角标？否——删除键无上滑语义；左滑超阈值进入快删流；
  快删中反向右滑进入恢复流；抬手落停）
- **DoD**：裁决状态机单测全绿；真机与微信输入法盲测删除手感（快删速度曲线、恢复粒度）

### T5 · 候选条 + 展开面板
- 收起条：5 候选 + 「˅」（锚点与桌面契约同语义）+ 细分隔线 + 模式标；横滑
- 展开面板：5 列网格纵滚翻页，75 池 3 页，页码指示；**四字候选完整显示**
- 交互：点候选上屏、空格=首选、回车=原样拼音、退格逐字母回退/空预编辑透传删除
- 接入 T2 治理管线与映射层（选词提交只走绝对序号）
- **DoD**：VI 截图验收（收起/展开 × 浅/深）；污染候选流端到端选词不错位

### T6 · InputMethodService 控制器
- 预编辑生命周期；中/EN 切换（取消预编辑不上屏、记忆上次中文模式）
- **inputType 识别**：密码/验证码/敏感变体 → 强制 EN 直出、禁学习、不出候选（§5.4）
- 横屏 `onEvaluateFullscreenMode`（关闭或自绘 extract view）；全面屏手势区底部 inset
- 失焦/切换输入框的状态清理（预编辑、候选、手势态全复位）
- **DoD**：场景清单全过：微信聊天、浏览器地址栏、备忘录、短信验证码、WebView 表单、
  横屏视频弹幕输入

### T7 · 性能与稳定性
- 预算：键盘冷启动 <300ms、按键→候选 P95 <100ms、常驻内存 <80MB
- monkey/遍历 30 分钟零崩溃；**崩溃日志审查：不含任何输入内容**（§9 红线）
- **DoD**：性能报告与崩溃-free 记录入库（后续版本回归基线）

### T8 · 验收与发布准备
- 真机矩阵：Pixel + 小米 + 华为 + OPPO/vivo（Android 10 / 12 / 14）
- 并排验收清单（ANDROID-BENCHMARK-ANALYSIS.md §六 共 9 条）逐项执行签字
- 产出营销资产：权限声明截图（无网络/无通讯录/无存储）、dumpsys 零联网证明
- 签名 APK；版本信息发布对接 `release/release.json` 唯一真相契约
- **DoD**：验收清单全绿；安装、升级、卸载、回滚路径各有明确结果

---

## 三、质量门禁（不可协商）

1. **UI 代码合入前**，其逻辑层（治理管线 / 映射 / 手势裁决 / 模式机）单测已存在且全绿。
2. detekt 零阻断项；ktlint 全量自动格式化；CI 不过不评审。
3. 真机矩阵不过不发布；性能预算不达标不发布。
4. 每个 T 完成时更新本文档 checkbox 与交接记录（对齐 Mac 端 MAC-HANDOFF 文档惯例）。

## 四、v0.1 明确不做（边界）

- 9 键、繁体 UI、设置四页（v0.1 无设置界面；主题跟随系统、高度默认标准，结构预留）
- 光标滑动、上滑清空/下滑撤回、智能加空格、双击句号、首字母大写（v0.2）
- GY Link、联网权限（v0.3）
- 手写、语音、表情、双拼、五笔、模糊拼音（远期）

## 五、排期提示

依赖序 T0→T1→(T2 ∥ T3)→T4→T5→T6→T7→T8；T2 与 T3 可并行。
无硬 deadline（质量优先），但每周固定产出：真机演示视频 + checkbox 更新。
