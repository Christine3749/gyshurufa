# GY 输入法 0.9.59

- 账户页新增 GY 账户登录：可直接使用现有 GSYEN / Supabase 账户的邮箱和密码登录，无需跳转浏览器。
- 登录成功后仅在 GyImeHost 进程内保留短期 access token；refresh token 以 Windows DPAPI 加密保存到当前用户的本机存储，不写入 settings.ini。
- 设置窗口每次打开都会在后台调用账户服务恢复会话；失效会提示重新登录，网络不可用时会保留已保存会话。
- 新增退出登录：先清除本机加密会话，再尽力通知账户服务，晚到的网络响应不会恢复已退出的状态。
