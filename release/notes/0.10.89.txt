• 新增 Windows 原生更新通知：发现新版本后只提示一次，不打断输入、密码框或候选窗。
• 通知提供“安装更新”和“稍后提醒”；稍后提醒默认延后 24 小时。
• 通知动作使用当前用户的 gyinput:// 协议注册，不依赖旧版本卸载器保留 HKLM 项。
• 点击安装后仍交给 AutoUpdate-GYInput.ps1，继续执行 SHA-256、Authenticode、UAC 和现有安装事务校验。
• 保留后台检查、设置页更新入口和版本隔离升级架构。
