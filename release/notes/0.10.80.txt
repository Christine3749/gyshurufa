• 安装器现在在持有统一事务锁时自动尝试 Finalizer；如果即时激活遇到锁定或临时失败，会自动保留 ONSTART 与 ONLOGON 任务兜底，其他电脑无需手动运行 PowerShell。
• 输入范围识别增强：密码、PIN、邮箱、登录名、网址和数字字段自动使用英文直输，不读取或上传字段内容。
• Tab 与 Shift+Tab 保持由当前应用处理，避免候选窗口吞掉浏览器、微信、Office 和终端的焦点导航。
• 安装后继续校验 Host、TSF、注册表、Logo、离线引擎和 active 状态，版本不一致时明确报告。
