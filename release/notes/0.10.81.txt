• 账户设置改为单一 GY 账户：删除容易引起误解的“本地标识”账号输入框，不再要求用户维护两个账号。
• 账户页重新整理为清晰的单卡片布局：标题、说明、邮箱、密码、登录按钮和状态信息分层排列；登录后显示账户、同步状态和退出登录。
• 保留 0.10.80 的安装器自动 Finalizer、ONSTART/ONLOGON 兜底、版本隔离、安装后校验和回滚保护。
• 保留密码、PIN、邮箱、登录名、网址和数字字段的英文直输，以及 Tab/Shift+Tab 应用接管行为。
