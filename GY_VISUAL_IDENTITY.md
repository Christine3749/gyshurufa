# GY 输入法视觉识别系统（VI）

版本：1.0  
状态：**已锁定的产品基线**  
适用范围：Windows TSF 候选窗、设置页、托盘图标、官网，以及 macOS、Android、iOS、Linux 后续版本。

## 1. 品牌一句话

**GY 输入法：安静、精确、跟手的系统级输入体验。**

它不是“发光的 AI 面板”，也不是聊天软件皮肤；它应像操作系统里本来就该有的一层高质量输入界面：克制、清晰、可信。

## 2. 不可改变的视觉骨架

当前候选窗是 GY 最有辨识度的界面，以下结构锁定：

```text
收起： [ 1 候选 ] [ 2 候选 ] … [ 5 候选 ] [ ˅ ] │ [ 简 / 繁 / EN ]
展开： 固定 5 列 × 最多 5 行候选网格（由键盘 ↓ 进入）
        [ ˄ ] │ [ 简 / 繁 / EN ]
```

- 近黑底、冷白字、细灰分割线、亮蓝选中态，是 GY 的主识别。
- 蓝色只用于“当前选中”和“可操作控件”；禁止用作大面积背景、渐变或霓虹装饰。
- 收起态的 `˅` 始终位于模式标记左侧，有独立固定格；其锚点在自身格子的左下方。它不能因为文字长度、屏幕宽度或样式微调而换位置。
- 展开态的 `˄` 位于底栏左侧，点击后收起；翻页仅在确有上一页/下一页时显示。
- 候选词四个汉字必须完整显示；不得以省略号替代正常四字词。
- 任何平台复刻此界面时，先复刻信息层级与留白，再复刻颜色。

## 3. 颜色令牌

| 令牌 | 数值 | 用途 |
| --- | --- | --- |
| `GY Ink` | `#111318` | 主候选窗、深色系统表面 |
| `GY Paper` | `#FAFAF8` | 浅色画布、反白界面 |
| `GY White` | `#F6F8FC` | 深色候选正文 |
| `GY Blue` | `#2863EB` | 选中候选、主动作 |
| `GY Disclosure` | `#5280E2` | 展开/收起箭头、模式标记 |
| `GY Divider` | `#31353D` | 深色边框、细分隔 |
| `GY Muted` | `#979DA9` | 快捷键、次级状态 |

### 候选窗样式

Windows 与 macOS 共用以下三套样式；它们是候选窗的配色选择，不改变
`GY Blue` 和 `GY Disclosure` 两个交互强调色。

| 样式 | 背景 | 正文 | 边框 | 次级文字 | 选中候选 |
| --- | --- | --- | --- | --- | --- |
| `GY 蓝夜`（默认） | `#111318` | `#F6F8FC` | `#31353D` | `#979DA9` | `#2863EB` |
| `暖白` | `#FCFCFB` | `#111318` | `#E1E5EB` | `#606C7A` | `#2863EB` |
| `石墨` | `#111318` | `#F6F8FC` | `#343943` | `#949EAE` | `#485160` |

## 4. 排版与空间

- 中文候选：半粗，优先清晰度，不使用艺术字体。
- 快捷键数字：10pt 半粗；模式标记：12pt 半粗；候选字默认 15pt 半粗。
- 圆角：外框 9dp；选中格 6dp。阴影极弱或不用阴影。
- 13/14 寸、常规屏、27 寸：展开窗固定 5 列、最多 5 行；仅调整窗口宽度与单元格间距，绝不改变列数。
- 13/14 寸宽度约 520–620px；普通屏约 620–720px；27 寸最大 820px。
- 单元格保持舒适固定宽度；禁止为了填满大屏把字距和列距无限拉大。

## 5. 托盘与品牌小图标

- GY Host 的托盘入口默认隐藏，不占通知区域；输入切换由 Windows 语言栏负责，设置由 GY 输入界面进入。
- 托盘仅用于诊断，显式设置 `GYINPUT_HOST_SHOW_TRAY=1` 才开启。
- 小图标使用 `native/installer/assets/gy-tray-icon.svg` 生成的 `gy.ico`：深墨黑底、白色 `GY`，字形须占小尺寸安全区的约 86% 宽度；不得缩小、改色或以其他图标替代。
## 6. 跨端迁移规则

Windows、macOS、Android、iOS、Linux 都必须共享本文件的颜色、信息层级和交互语义；允许因平台规范改变控件尺寸、系统字体和安全边距，但不得改变：

1. 近黑底 + 冷白字 + 精确蓝的关系；
2. 收起态“候选 → 展开箭头 → 分隔线 → 语言状态”的顺序；
3. 展开态候选优先、控制区克制的优先级；
4. GY 标准字标：深墨黑 `#111318` 上白色 `GY`，或浅底上深墨黑 `GY`。

## 7. 禁止项

- 禁止彩虹、荧光绿、网格背景、赛博朋克 HUD、玻璃拟态大光晕。
- 禁止把表情、罕见字或繁体字作为默认高频候选。
- 禁止为“AI 感”增加会干扰打字的动画、卡片或提示。
- 禁止未经产品决定，调整展开箭头与模式标记的相对位置。
- 禁止模仿任何竞品的图标、候选窗比例、按钮形状或文案。

## 8. 代码映射与验收

Windows 候选窗的当前实现令牌位于 `native/src/CandidateWindow.cpp`：

- `kBrandInk`、`kBrandPaper`、`kGyBlue`、`kDisclosureBlue`
- `kDisclosureInsetX`、`kDisclosureInsetBottom`：收起态箭头的位置锁定点

任何调整候选窗的 PR/提交都必须检查：

1. 13/14 寸、常规屏、27 寸各一张截图；
2. 收起态与展开态各一张截图；
3. 四字候选完整显示；
4. 箭头仍在锁定格的左下方，且在 `简/繁/EN` 左侧；
5. 蓝色只出现在选中态和交互点。

> 原则：功能可以持续进化，GY 的表面语言不应每个版本重新设计。
