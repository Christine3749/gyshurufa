# GY 输入法产品标准（跨平台唯一基线）

版本：1.1
状态：**产品已确认，跨端开发必须遵守**
适用范围：Windows、macOS、Android、iOS、Linux、官网、安装包、账户与同步服务。

> GY 输入法，AI 时代的输入法。
> 先让每一次打字稳定、自然、可预期；AI 只在用户明确需要时出现。

## 1. 产品本质

GY 是原生系统输入法，不是网页输入框、浏览器插件或聊天式 AI 面板。

- 基础拼音、候选、学习、短语和中英文切换在本地完成，断网可用。
- 输入路径不联网：按键、预编辑、候选刷新、选词、上屏不能调用 AI 或云服务。
- AI 是主动调用的写作工具，只处理用户明确选中的内容；必须可见、可取消、可关闭。
- 好输入法不抢注意力：首选可信、反馈快速、界面稳定、没有广告和无关内容。

## 2. 三种输入模式：简体／繁体／EN

| 模式 | 行为 |
| --- | --- |
| 简体 | 拼音、简体候选、中文标点。默认模式。 |
| 繁体 | 拼音、繁体候选、中文标点。繁体只在此模式出现。 |
| EN | 真正英文直出：不弹中文候选、不拦截英文字母、数字、英文标点、Enter、Tab、Backspace、复制粘贴或应用快捷键。 |

- 单独按 Shift 或已确认的系统快捷键只切换模式，不提交候选、不改变文本。
- 模式状态在 Chrome、Arc、Firefox、ChatGPT、微信、记事本、VS Code、地址栏和应用切换后必须一致。
- 从中文预编辑切到 EN 时，取消未确认的拼音；不能偷偷上屏中文。
- 密码框、终端、代码编辑器、网址栏优先原样输入，禁用自动纠错和自动 AI 行为。

## 3. 候选与键盘交互（已锁定）

### 3.1 候选质量

- 候选池上限是 **75**，不是必须填满的数量；可以是 0、6、18、31 或 75。
- 原始引擎可读取更多候选作为过滤缓冲；进入产品候选池的必须是实际合格候选，禁止空位填充、乱码、表情、符号、重复词。
- 简体模式过滤繁体；繁体模式只输出繁体；默认高频候选不出现表情、罕见字或无意义怪词。
- 首选五个候选优先保证自然和可信。第 26–75 位采用更严质量门槛：宁可没有下一页，也不塞低质量单字或残片。
- 本地学习只能提升用户真实选择过的词；用户词库和常用短语优先，但仍须遵守当前简繁模式。

### 3.3 展开网格的确认契约（Windows × macOS 共同规则）

- 只有用户主动打开 5 × 5 网格后，方向键才用于网格移动；高亮必须与实际待提交候选保持一致。
- 展开态按 Enter 或 Space 必须提交当前高亮候选；不能出现“蓝色高亮能移动、但按键无法选词”的状态。
- 收起态不改变既有 Enter 行为：它继续原样上屏预编辑拼音，避免破坏英文直出或用户已有习惯。
- macOS 采用 InputMethodKit 实现相同语义；Windows 采用 TSF/Host 实现相同语义。两端禁止按底层实现差异改变这套键盘契约。
### 3.2 窗口与分页

- 收起态固定显示一行 **5 个**高频候选。
- 展开态固定 **5 列 × 最多 5 行**，单页最多 25 个；75 个候选最多三页。
- 候选不足 25 个时只显示真实候选，不能为了排版或凑页数加入假候选。
- 单击展开箭头可展开；键盘首次按 `↓` 也必须展开。
- 展开后 `← → ↑ ↓` 在网格内移动选中项；到页边界时跨页同列移动。
- 收起态 `Enter` 保留原样上屏拼音；展开后 `Enter` 或 `Space` 必须提交当前高亮候选，不能只移动蓝色高亮而无法选词。
- `PageUp / PageDown` 翻整页；第一页第一行再按 `↑` 收回单行。`Esc`、选择候选、开始新拼音后按既定规则收起。
- 四字词必须完整显示，禁止以省略号替代正常四字候选。

## 4. 已锁定的 GY 视觉与交互

视觉唯一基线是 [GY_VISUAL_IDENTITY.md](gy输入法/GY_VISUAL_IDENTITY.md)。本文件不重复定义颜色或尺寸；所有平台必须以该文件为准。

未经明确产品决定，**不得修改**：

1. GY 标准 Logo、托盘小图标的 `GY` 字标；
2. 收起态的候选、箭头、分隔线、模式标记顺序；
3. 箭头位置、颜色、开口、锚点或所在格；
4. 近黑底、冷白字、GY Blue 选中态、细灰分隔线；
5. 已确认的设置页结构、候选窗比例、圆角和交互；
6. 5 列 × 最多 5 行规则。

功能开发只能最小化增加所指定能力。任何视觉或交互变更必须先更新 VI 规范、给出跨屏截图，再取得产品确认。

## 5. 平台实现边界

### Windows

- Windows 采用 TSF 核心 DLL + 独立 Host 架构。
- TSF DLL 保持小而稳定：键盘接入、模式读取、预编辑、文本提交和与 Host 通讯。
- 候选质量、词库、学习、设置、AI、同步、诊断等持续演进功能放入 Host 或独立服务。
- 不得强行从其他应用进程卸载已加载的 DLL；核心更新应明确提示关闭受影响应用，必要时注销或重启 Windows。

### macOS

- macOS 必须是原生 InputMethodKit 输入法，不复制 Windows TSF DLL、命名管道、注册表或安装器代码。
- Mac 复用 GY 的产品语义、Rime 数据、候选质量规则、模式规则、VI、账户与同步契约；用 macOS 原生输入源、钥匙串和签名/公证实现。
- 第一阶段可使用系统候选窗确保兼容性；定制 GY 面板前，必须保证相同的五候选、模式、选词和分页语义。
- macOS 核心 Bundle 被运行中应用加载时也不可强制热替换；核心更新应提示退出受影响应用或注销，不能假装已激活。

### Android、iOS、Linux

- 各端分别使用原生输入法框架；共享产品语义，不复制其他平台的底层实现。
- iOS、Android、Linux 尚未完成时只能显示“开发中／路线图”，禁止假下载链接。

## 6. 隐私、AI 与账户

- 默认离线；输入内容、候选、词频和剪贴板不可静默上传。
- 跨设备词库、短语、设置、剪贴板同步必须由用户登录并明确开启。
- Windows 令牌使用 DPAPI / Credential Manager；macOS 令牌使用 Keychain；任何客户端不得存储密码、Service Role Key 或明文 API 密钥。
- AI 请求走独立受控服务：身份验证、限流、最小化日志、内容说明、用户主动触发。

## 7. 更新、版本与发布

- `release/release.json` 是唯一发布真相。官网、下载 Worker、Windows、macOS 和安装页不得硬编码版本号。
- 每一发布版本不可覆盖重打包：修复只发布新版本；R2 版本目录不可变，`latest.json` 只在验证后切换。
- Windows DLL、Host、安装包和哈希必须版本一致；Mac PKG、签名、公证和哈希必须版本一致。
- 更新完成必须分状态显示：**包已校验 / Host 已切换 / 核心已激活**。若 DLL 或 Bundle 仍被应用占用，必须诚实提示，而不是显示“已完成”。
- 正式公开包必须使用可信签名；Mac 正式包需 Developer ID 签名、公证与 stapling。

## 8. 跨端验收清单

任何平台进入公测前，至少验证：

1. 简体、繁体、EN 三模式正确；EN `hello, world!` 原样直出。
2. Shift、Enter、Tab、Backspace、复制粘贴、常用快捷键不被错误拦截。
3. 拼音、候选、选择、取消、翻页、5×5 展开与收起符合第 3 节。
4. Chrome/Arc、Firefox、ChatGPT、微信、记事本、Office、VS Code、终端等常用场景稳定。
5. 13/14 寸、普通屏、27 寸的收起与展开截图符合 VI。
6. 首次安装、旧版升级、回滚、卸载、受影响应用仍打开时的升级都有明确结果。
7. 基础输入断网可用，AI 未被主动调用时不产生网络请求。

## 9. 变更控制

开发者在开始任何输入法改动前必须先读：

1. 本产品标准；
2. [GY_VISUAL_IDENTITY.md](gy输入法/GY_VISUAL_IDENTITY.md)；
3. 平台对应的架构与发布文档。

如果需求不明确，默认**不改变现有视觉、交互、Logo、箭头、设置页或已确认功能**。先提出最小变更方案，等待产品确认后再实现。
