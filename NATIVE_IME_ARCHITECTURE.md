# GY 输入法：原生 Windows 实施蓝图

## 结论

当前工程是 React/Vite 的交互原型，不是 Windows 输入法。它不能接收系统按键、创建编辑会话或向 Word、浏览器、微信等程序提交文字。

正式产品使用 [三层架构契约](release/ARCHITECTURE_CONTRACT.md)，拆分为三个边界明确的模块：

```text
Windows 应用程序
        │ TSF 编辑会话
        ▼
GYTSF.dll  ───────────►  GYInputEngine / GyImeHost
（按键、预编辑、上屏）          （分词、候选、排序、学习、候选窗）
                                       │
                                       └── 本地 SQLite / 词频库
                                                │ 本地 IPC
                                                ▼
                                   GYAgent.exe（账户、同步、更新、显式 AI）
```

## 不可妥协的产品原则

1. **基础输入离线可用**：拼音转写、候选、用户词频、短语、剪贴板历史均不得依赖网络。
2. **输入路径零网络**：任何按键处理、候选刷新和上屏流程都不调用 AI API。
3. **AI 是显式工具**：仅处理用户选中的文本；弹出说明、可取消、可关闭、可审计。
4. **每次按键有预算**：按键到候选窗更新的 P95 小于 16 ms；候选查询不做阻塞 I/O。
5. **故障不影响打字**：词库或 AI 服务崩溃时，TSF 前端降级为基础拼音输入，不阻塞目标应用。

## 推荐技术选型

| 层级 | 选择 | 原因 |
| --- | --- | --- |
| 系统接入 | C++20 + Windows TSF / COM | 这是 Windows 原生输入法标准接口，能进入任意 TSF 目标应用。 |
| 输入引擎 | 第一阶段接入 librime；长期保留 GY 排序层 | librime 是 BSD-3-Clause 的跨平台 C++ 输入引擎，能快速得到成熟的拼音、OpenCC 与词库能力。 |
| 自有能力 | GY 用户学习、上下文排序、短语、领域词典 | 这是最终差异化，而不是重写成熟基础拼音解析。 |
| 存储 | SQLite + DPAPI 加密敏感设置 | 本地、可迁移、可清除；不把输入记录写进日志。 |
| 候选窗 | Win32 / DirectWrite / Direct2D | 低延迟、DPI 正确、可贴合文本光标。 |
| 账户与同步 | `GYAgent.exe` + Supabase Auth | 登录、设备和网络与输入 Host 隔离；令牌只存 DPAPI。 |
| 设置应用 | WinUI 3 或 Tauri 2 | 与 TSF DLL 隔离；当前 React 原型可以迁移为设置页面，而不能作为输入前端。 |
| 安装与更新 | MSIX 或签名 MSI + 独立更新器 | 输入法需要管理员级安装体验、卸载和版本回滚。 |

> 不直接分发或改名 Weasel：它是 GPLv3。若采用 Rime，优先评估 BSD-3-Clause 的 librime，并单独完成依赖与词库许可证清单。

## 真正好用的输入体验

### 第一版必须做对

- 全拼、简拼、混输；`zhongguo`、`zg`、`zhong guo` 都可用。
- 连续句输入与动态分词，空格确认、数字选词、左右键移动、退格回退必须稳定。
- 精确匹配优先；用户主动选词的学习权重高于通用词频。
- 中英文智能切换：URL、邮箱、代码、路径、版本号、CamelCase 不被错误转中文。
- 自定义短语、专业词库、模糊音和双拼是可选项，不干扰默认用户。
- 繁简转换、标点模式、全半角、日期时间和计算器短语等基本能力。

### 早期 Google 输入法值得借鉴的方向

- 极少干扰：候选窗短、稳定、首选可信，不把皮肤/表情/广告塞进输入路径。
- 预测以“少按一次键”为目标，而不是强行替用户写句子。
- 输入可预期：用户能理解为什么某词排第一，并能用一次选择纠正它。

### 以后才做的 AI 能力

- 选中文本后按快捷键：润色、翻译、改写、摘要、回复建议。
- 用户授权的应用上下文，例如在邮件、IDE、客服系统中调用相应工作流。
- AI 结果只能进入预览面板；用户点击“插入”后才写入目标应用。

## 交付顺序

1. **M0：可安装的 TSF 骨架** — 注册、启用、禁用、卸载、诊断日志；暂不追求 AI。
2. **M1：可靠拼音** — 预编辑、候选窗、上屏、退格、翻页、全拼/简拼、离线词库。
3. **M2：可控个性化** — 本地学习、用户词库、导入导出、隐私控制与一键清除。
4. **M3：生产级体验** — 高 DPI、多屏、Office/浏览器/Electron/游戏兼容测试、崩溃恢复、签名安装包。
5. **M4：显式 AI 助手** — 独立进程、授权与限额、结果预览、可完全关闭。

## 验收门槛

- Windows 10/11、Office、Edge/Chrome、微信、VS Code、Electron 软件中均可输入。
- 连续输入 10 万字符无卡顿、无词库损坏、无内存持续增长。
- 飞行模式下完整完成 M1/M2 功能。
- 未点击 AI 时抓包为零；禁用 AI 后没有相关进程或网络请求。
- 安装、卸载、切换输入法、异常崩溃均不影响系统现有输入法。

## 当前原型如何处理

- `src/components/CandidateWindow.tsx`：保留为视觉规格，不作为系统候选窗实现。
- `src/lib/dictionary.ts`：保留为产品文案/演示数据；正式词库转为受许可证约束的数据文件。
- `server.ts`：不能进入输入法安装包；AI 请求今后移动到独立的、用户可见且可禁用的 GYAgent 进程。
