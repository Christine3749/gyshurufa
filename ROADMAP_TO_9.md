# GY 输入法：9/10 可用度升级目标

## 产品目标

在 Windows 上做到“安装自然、任意常见输入框稳定、输入不打断快捷键、候选窗轻且清晰、默认离线”，达到可邀请真实用户参与 Beta 的 9/10 体验。

## 当前发布优先级

| 优先级 | 目标 | 可量化完成条件 |
|---|---|---|
| P0 | 输入不会打断工作 | Ctrl/Cmd 类组合键、Alt、Win、Shift+Tab 与 Ctrl+Shift 均不被 GY 吞掉；单独 Shift 切换中英文。 |
| P0 | 候选窗可预测 | 5 项横向候选、左右翻页、鼠标点击安全回传 TSF、光标悬停可点击项目显示手型。 |
| P1 | 常见应用稳定 | Chrome/Edge、微信、Word、记事本、VS Code、Electron、管理员程序逐项人工回归并记录结果。 |
| P1 | 本地可控 | 设置、账号显示、主题/字号、短语、学习数据清除与备份/恢复均真实生效且离线。 |
| P2 | 优雅升级 | Host/词库/外观更新不要求重启；极少数 TSF 核心更新仅提示关闭并重开正在输入的应用。 |
| P2 | 发布可信 | EXE 与 DLL Authenticode 签名、SHA-256、回滚、崩溃诊断（需用户同意）和正式卸载闭环。 |
| P3 | 输入质量竞争力 | 真实打字语料评测：长句、模糊音、英文混输、词频学习、纠错和延迟均有基准与回归门槛。 |

## 9/10 发布门槛

- 10 个真实应用/场景回归连续 3 个版本无 P0/P1 故障。
- 自动化原生测试、安装/升级/卸载测试全部通过。
- 常规 Host 更新无需注销或重启；TSF 核心更新无数据丢失且有明确“关闭并重开应用”提示。
- 关键输入操作端到端延迟目标 < 16ms；候选提交不使用 SendInput，不会写入错误窗口。
- 安装器和 DLL 均完成真实代码签名，公开下载附哈希与隐私说明。

## 版本化规则

- `GyIme.dll`：极小 TSF 连接器。仅兼容协议或安全修复才升级。
- `GyImeHost.exe`：候选窗、设置、排序、词库和本地功能，允许频繁更新与回滚。
- `rime-data`、短语、学习数据：与程序版本独立，升级绝不删除用户数据。
- 每次发布必须先跑独立 Health Check；失败则保留旧 Host，不切换到新版本。