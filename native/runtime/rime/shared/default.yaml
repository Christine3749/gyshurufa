# GY local runtime: Simplified Chinese pinyin only.
# encoding: utf-8
config_version: '2.1'
schema_list:
  - schema: luna_pinyin
menu:
  # Keep a larger engine page than the 5 × 5 visual panel; GY filters it down.
  page_size: 96
